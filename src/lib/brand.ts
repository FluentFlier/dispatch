/** App-wide product name — single source of truth for UI copy. */
export const PRODUCT_NAME = 'Content OS';

export const SITE_TITLE = `${PRODUCT_NAME} — Your content engine, trained on you`;

export const CTA_OPEN_APP = `Open ${PRODUCT_NAME}`;
